根据您的考试大纲要求，我为您设计了一份不包含编程题的Java试题，总分100分。题型和分值分布调整为：选择题25题（50分）、判断题10题（10分）、简答题4题（20分）、问答题2题（20分）。

---

Java程序设计试题

一、选择题（每题2分，共50分）

1. Java中，下列哪个数据类型不是基本数据类型？
   A. int

   B. String

   C. boolean

   D. double

2. 关于Java异常处理，以下说法正确的是？
   A. finally块必须和try-catch一起使用

   B. catch块可以捕获多种类型的异常

   C. throws关键字用于在方法内部处理异常

   D. RuntimeException必须被显式捕获或声明

3. 下列关于封装的说法，错误的是？
   A. 封装可以隐藏对象的内部实现细节

   B. 使用private修饰符实现封装

   C. 封装会降低代码的安全性

   D. 提供公共的getter/setter方法访问私有属性

4. 在Java中，继承使用哪个关键字？
   A. implements

   B. extends

   C. inherit

   D. super

5. 关于多态的描述，正确的是？
   A. 多态只能通过方法重载实现

   B. 父类引用可以指向子类对象

   C. 多态会降低程序的灵活性

   D. final方法可以被重写以实现多态

6. 下列哪个接口不是java.util.Collection的子接口？
   A. List

   B. Set

   C. Map

   D. Queue

7. ArrayList和LinkedList的主要区别是？
   A. ArrayList线程安全，LinkedList线程不安全

   B. ArrayList基于数组实现，LinkedList基于链表实现

   C. ArrayList不允许重复元素，LinkedList允许

   D. ArrayList元素有序，LinkedList元素无序

8. HashSet底层实现主要依赖于？
   A. ArrayList

   B. LinkedList

   C. HashMap

   D. TreeMap

9. 关于HashMap和TreeMap的描述，正确的是？
   A. HashMap线程安全，TreeMap线程不安全

   B. TreeMap基于哈希表实现，HashMap基于红黑树实现

   C. HashMap不允许null键，TreeMap允许

   D. TreeMap可以对键进行自然排序

10. 下列哪个接口用于自定义对象排序？
    A. Comparable

    B. Comparator

    C. Iterable

    D. Collection

11. 关于Lambda表达式，以下说法错误的是？
    A. Lambda可以简化匿名内部类的编写

    B. Lambda表达式可以有多个参数

    C. Lambda表达式必须指定参数类型

    D. Lambda表达式可以作为方法参数传递

12. 创建线程的方式不包括？
    A. 继承Thread类

    B. 实现Runnable接口

    C. 实现Callable接口

    D. 实现Threadable接口

13. 关于线程安全的描述，正确的是？
    A. 所有线程共享的变量都是线程安全的

    B. StringBuilder是线程安全的

    C. 使用synchronized关键字可以保证线程安全

    D. ArrayList是线程安全的

14. 下列哪个方法可以唤醒等待的线程？
    A. sleep()

    B. wait()

    C. notify()

    D. join()

15. JDBC中，用于执行SQL查询语句的接口是？
    A. Statement

    B. Connection

    C. ResultSet

    D. PreparedStatement

16. 关于TCP和UDP的说法，错误的是？
    A. TCP是面向连接的，UDP是无连接的

    B. TCP保证数据可靠性，UDP不保证

    C. UDP传输速度比TCP快

    D. TCP和UDP使用相同的端口号范围会产生冲突

17. Java中，用于表示IP地址的类是？
    A. Socket

    B. ServerSocket

    C. InetAddress

    D. URL

18. 关于Lambda表达式和匿名内部类的区别，正确的是？
    A. Lambda可以访问所有外部变量

    B. Lambda会在编译时生成独立的类文件

    C. Lambda只能用于函数式接口

    D. Lambda可以使用this引用匿名类本身

19. 下列哪个集合类是线程安全的？
    A. ArrayList

    B. HashMap

    C. Vector

    D. HashSet

20. 关于JDBC访问数据库的步骤，正确顺序是？
    A. 加载驱动→建立连接→执行SQL→处理结果→关闭连接

    B. 建立连接→加载驱动→执行SQL→处理结果→关闭连接

    C. 加载驱动→执行SQL→建立连接→处理结果→关闭连接

    D. 建立连接→执行SQL→加载驱动→处理结果→关闭连接

21. TCP通信中，服务器端使用的类是？
    A. Socket

    B. ServerSocket

    C. DatagramSocket

    D. DatagramPacket

22. 关于volatile关键字的作用，描述正确的是？
    A. 保证操作的原子性

    B. 禁止指令重排序

    C. 保证变量的可见性

    D. 替代synchronized同步

23. 下列哪个方法会导致线程进入阻塞状态？
    A. yield()

    B. start()

    C. sleep()

    D. isAlive()

24. 关于PreparedStatement的优点，不包括？
    A. 防止SQL注入

    B. 预编译提高性能

    C. 代码可读性差

    D. 支持参数化查询

25. UDP通信中，用于接收数据的类是？
    A. DatagramSocket

    B. DatagramPacket

    C. ServerSocket

    D. Socket

---

二、判断题（每题1分，共10分）

1. Java中，子类可以重写父类的private方法。（  ）

2. HashMap的键值对是无序的，而LinkedHashMap可以保持插入顺序。（  ）

3. Lambda表达式的参数类型必须显式声明。（  ）

4. 实现Runnable接口创建线程比继承Thread类更灵活。（  ）

5. StringBuffer和StringBuilder都提供了线程安全的字符串操作。（  ）

6. 在JDBC中，Connection对象可以创建多个Statement对象。（  ）

7. TCP协议的三次握手是为了建立可靠的连接。（  ）

8. 使用synchronized修饰静态方法时，锁定的是当前对象。（  ）

9. TreeSet集合中的元素必须实现Comparable接口。（  ）

10. UDP通信不需要建立连接，直接发送数据包即可。（  ）

---

三、简答题（每题5分，共20分）

1. 请简述ArrayList和LinkedList的区别，并说明分别在什么场景下使用更合适。

2. 解释Java中的多态概念，并举例说明方法重写如何实现多态。

3. 请说明创建线程的两种主要方式，并比较它们的优缺点。

4. 简述JDBC访问数据库的基本步骤，并说明Statement和PreparedStatement的区别。

---

四、问答题（每题10分，共20分）

1. 请详细阐述TCP和UDP两种网络通信协议的区别，包括：
   - 连接方式
   - 数据传输可靠性
   - 适用场景
   - Java中实现TCP和UDP通信分别使用哪些核心类？并描述TCP通信的完整过程（从服务器启动到客户端连接、数据交互）。

2. 集合框架是Java的核心内容之一，请系统阐述：
   - List、Set、Map三大接口的特点和区别
   - 分别列举每个接口的常用实现类（至少2个）
   - 解释Comparable和Comparator接口的作用及区别
   - 如何使用Lambda表达式简化集合的排序和遍历操作（举例说明）

---

